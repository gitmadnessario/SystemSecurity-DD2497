Build the project using realesetools
Boot Minix
Login as root
Create a user(user add -m XXXX)
Give him a password(passwd XXXX)
Logout(exit)
Login as the new user

/* Sometimes safecopy fails and spills the message on the screen.
    It does not affect anything
*/

Create a text file(vi XXXX)
Right something in the file and save
Copy the file
Dump the content of the file(cat XXXX) -- The outpout should 
    have the first letter different, letter h(demonstrates a different
    read/decryption)
Open the other file -- The content should be different in the first 
    letter, letter c (demonstrates a different write/encryption)

